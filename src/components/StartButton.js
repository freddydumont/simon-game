import React from 'react';
import start from '../images/start.svg';
import startPressed from '../images/start_pressed.svg';

const StartButton = () => (
  <img className="switch" src={start} alt="Start Button" />
);

export default StartButton;